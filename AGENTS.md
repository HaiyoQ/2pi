# 项目协作说明

## 沟通方式

* 用户懂一点编程概念，但不以写代码为主；说明问题和方案时使用半专业、新生也可理解的表达。

## UI 与交互

* UI 界面文案优先使用中文。

## 设计原则

* 优先简单可靠，不过早优化。
* 重要状态变更要考虑幂等性，避免重复点击、重复调用或异常重试造成脏数据。
* 新增功能优先做成可扩展能力，尽量通过扩展接入 Agent 核心，而不是把逻辑硬编码到核心流程里。
* 修改现有能力时，优先遵循当前项目结构和已有实现风格，避免引入不必要的新框架或复杂抽象。
* 对涉及本地文件、模型密钥、会话记录的改动，要明确数据保存位置和读写边界。
* 代码报错，先找真根因。若问题简单且修复无连锁破坏，给最小修复；若简单修复会引发连带问题，指明风险，不盲目给方案。禁止过度设计。

## 当前边界

* 目前不做、不考虑沙箱/容器隔离、OAuth 认证。
* 当前项目以本地桌面应用为主，没有独立后端服务和数据库模型时，不要强行设计服务端或数据库层。

## 功能文档要求

* 后续新增、调整或删除独立功能、模块时，必须站在第三方客观的角度去同步更新 `FEATURES.md`。
* `FEATURES.md` 只写功能说明和设计记录，绝对不要粘贴代码。
* 文档描述要让半专业人士也能看懂：说明用途、流程、关键文件和配置，不写成纯代码注释。
* 每项功能按以下字段维护：

  * **状态**：规划中 / 开发中 / 已完成，时间具体到小时。
  * **需求背景**：为什么要做，解决什么问题。
  * **设计思路**：技术选型、架构位置、核心逻辑。
  * **关键决策**：为什么这样设计，主要权衡点是什么。
  * **核心流程**：用户或系统如何一步步完成这件事。
  * **涉及模块/文件**：前端页面、主进程 IPC、运行时、共享类型、数据存储等。
  * **配置项**：环境变量、默认值、本地配置文件、特性开关等。
  * **变更记录**：日期 + 改动摘要。
* 如果新增功能涉及持久化，要在文档中说明数据保存位置、格式和生命周期。
* 如果功能通过扩展接入 Agent 核心，要记录扩展入口、注册方式和与核心运行时的关系。

