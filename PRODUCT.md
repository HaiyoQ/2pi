# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

2pi 面向在个人电脑上维护本地项目的个人开发者。用户具备基础编程概念，希望在配置模型与项目目录后，直接把读取、分析、修改和验证代码的任务交给 Agent 完成。

## Product Purpose

2pi 是一个本地桌面编程 Agent。它把模型供应商配置、项目上下文、任务执行过程和历史会话放在同一个工作空间内，让用户可以发起任务、观察执行、停止任务并恢复过去的工作。

成功意味着用户能快速确认 Agent 正在处理哪个项目、使用哪个模型和执行档位，能理解运行过程中发生了什么，并能在结果出现后继续下一步。

## Positioning

产品以“配置模型后即可工作的本地编程 Agent”为核心位置。它不提供独立云端账号体系或项目数据库；供应商密钥、全局 Agent 参数和会话历史都保留在本地桌面应用的数据目录中。

## Operating Context

- 用户先添加模型供应商、密钥和模型，再选择本地项目目录。
- 用户以自然语言描述开发任务，Agent 根据执行档位读取项目、搜索代码、运行命令或修改文件。
- 用户在任务过程中查看思考、工具调用、失败、重试和最终结果，并可主动中止运行。
- 用户可以创建、恢复会话，并在不同供应商和模型之间切换。运行期间设置与模型保持锁定，变更从下一次任务起生效。

## Capabilities and Constraints

- 支持多供应商、自定义协议、连接测试、模型发现和模型切换。
- 支持“只读”和“全自动”两种执行档位；默认使用全自动。
- 只读档仅允许读取、搜索和列举文件，遇到修改任务时只分析并给出方案。
- 全自动档可使用读取、搜索、命令、编辑和写入工具；高级设置可逐项关闭工具。
- Agent 参数为全局作用域，不按项目或会话单独保存。
- 当前不设计账号、独立后端、数据库、OAuth、沙箱或容器隔离。
- 设置保存在 Electron `userData/settings.json`，会话保存在 `userData/sessions`；密钥只在主进程解密。
- 桌面应用继续使用 Electron、Vue 3、Pinia、Element Plus 和 Lucide。

## Brand Commitments

产品主称统一为“2pi”，正式工作区使用纯文字品牌，不配置独立品牌图标。界面文案优先使用中文，不再把“编程助手”作为主要品牌名称。

## Evidence on Hand

仓库现有实现包含会话创建与恢复、任务取消、多供应商、自定义协议、连接测试、模型发现、模型切换、设置保存和本地会话记录。当前没有应当在界面中使用的客户评价、商业数据或性能基准，后续设计不得虚构。

## Product Principles

- 工作状态始终可见：项目、模型、执行档位和运行状态不应藏在深层设置中。
- 运行过程可理解：读取、搜索、命令、修改、失败和重试需要被清楚区分，长内容可按需展开。
- 本地边界明确：密钥、设置和会话的保存位置及读写边界必须可解释。
- 默认直接完成任务：全自动模式不逐次请求工具审批，同时保留停止任务和只读模式。
- 简单可靠优先：沿用现有桌面技术栈，不为当前范围引入服务端或新框架。

## Accessibility & Inclusion

界面应支持键盘操作、清晰焦点、减少动效偏好和最小窗口尺寸。状态不能只依赖颜色表达，长路径、长命令和长错误信息必须保持可读。
