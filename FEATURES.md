# 功能说明与设计记录

## Windows 本地编程 Agent MVP

**状态**：已完成（2026-07-25 14:35 CST）

**需求背景**：面向 Windows 本地项目提供一个可直接使用的编程助手。用户需要在桌面应用内选择项目、配置模型、发送任务，并通过明确的执行档位控制 Agent 是否能修改文件或执行命令，不依赖独立后端、数据库、账号系统或沙箱。

**设计思路**：应用采用 Electron、Vue 3、Vite、Pinia 和 Element Plus。renderer 只负责界面与状态展示，preload 暴露固定的类型化 IPC，主进程中的 `AgentRuntime` 独占 Agent SDK、文件系统和模型密钥。`@earendil-works/pi-ai`、`pi-agent-core` 与 `pi-coding-agent` 固定为 `0.81.1`。执行档位和工具开关在主进程转换为 SDK 工具允许列表，Agent 核心不包含桌面 UI 逻辑。

**关键决策**：只读档只允许读取、搜索和目录查看，主进程始终过滤 `bash`、`edit`、`write`；全自动档按高级工具开关直接执行，不再逐次审批。Windows 终端工具使用自定义 PowerShell 执行适配器，并以 UTF-8 输出；不要求用户额外安装 Git Bash。API Key 只通过 IPC 进入主进程，使用 Electron `safeStorage` 加密后保存，不写入 renderer 日志或会话。

**核心流程**：用户先选择工作目录，并在供应商配置中心添加服务商、配置密钥与模型；随后选择执行档位与当前模型，新建或恢复会话并发送任务。每次发送生成不可变的运行合同，Agent 输出按回合时间线显示；用户可查看思考状态、工具链、重试、错误和用量，并可随时中止运行。

**涉及模块/文件**：`src/renderer` 包含任务时间线、会话和设置界面；`src/preload/index.ts` 是唯一 renderer 到主进程桥梁；`src/shared/contracts.ts` 定义稳定的 IPC、运行合同与事件类型；`src/main/ipc.ts` 校验 IPC 参数；`src/main/runtime/agent-runtime.ts` 管理 SDK 会话、模型、工具、事件和取消；`session-timeline.ts` 负责恢复可见时间线；`settings-store.ts` 管理设置与密钥；`tests` 覆盖事件转换、参数校验、执行档位、设置迁移和会话恢复。

**配置项**：设置文件位于 Electron `app.getPath("userData")/settings.json`，包含 v3 供应商档案、当前模型、工作目录、全局 Agent 参数及加密后的密钥数据，应用卸载或用户清除应用数据前持续存在。SDK 会话以 JSONL 保存在 `app.getPath("userData")/sessions`，生命周期由用户数据目录管理；项目文件始终只位于用户选择的工作目录。SDK 内部配置位于 `app.getPath("userData")/agent`。当前没有环境变量、远端配置、数据库或特性开关。

**变更记录**：2026-07-24 18:00 CST，创建 Windows MVP 工程、类型化 IPC、AgentRuntime、会话与设置持久化、中文桌面界面及基础测试。2026-07-24 20:38 CST，显式关闭 Electron 默认 renderer sandbox，修复 ESM preload 未执行、`window.agent` 无法注入的问题，并增加窗口配置回归测试。2026-07-25 14:35 CST，以只读/全自动执行档位替换逐次审批，并接入可恢复的运行时间线。

## 多供应商与自定义模型设置

**状态**：已完成（2026-07-25 15:04 CST）

**需求背景**：原有设置只能保存一个服务商、一个模型和一份 API Key，无法同时管理 OpenAI、Anthropic、本地模型和企业网关。用户也无法在不修改代码的情况下接入 OpenAI 兼容服务或带额外请求头的内部接口。

**设计思路**：模型设置升级为供应商配置中心。页内使用左侧供应商标签列表与右侧参数编辑区；左栏同时容纳已添加供应商、SDK 内置目录和自定义供应商入口，顶部搜索会将列表收敛为匹配结果。内置目录只提供名称、协议和 Base URL，不把 SDK 静态模型目录带入用户配置；模型由用户连接真实供应商接口后拉取。每个已保存供应商拥有独立名称、协议、Base URL、模型列表、API Key 和请求头。自定义供应商通过 `ModelRuntime.registerProvider()` 注册到 Agent 运行时，支持 OpenAI Chat、OpenAI Responses、Anthropic Messages 和 Google Generative AI 四种协议，并为手动模型补充文本输入、128K 上下文、16K 最大输出和零成本等 SDK 必需元数据。

**关键决策**：供应商结构在设置 v2 中引入，目前由 v3 设置文件承载并继续自动迁移旧版 `provider/modelId/encryptedApiKey`，保留原 OpenAI 选择与密钥；新安装不预置供应商。内置供应商可以借用 SDK 中的模型元数据识别兼容协议，但 renderer 收到的目录模型数组固定为空，避免静态模型列表过期或让用户误以为已经验证可用。API Key 与整组请求头值分别使用 Electron `safeStorage` 加密，renderer 和 IPC 返回值只包含 `hasApiKey`、请求头名称及是否已保存，不返回秘密值。连接测试结果可将认证错误归因到 API Key，将地址、网络和超时错误归因到 Base URL；无法可靠归因的服务错误仍显示整体反馈，不猜测字段。模型的“推理”开关只声明该模型支持 reasoning/thinking 参数，不控制流式输出或工具调用。自定义供应商使用 `custom-<uuid>` 稳定 ID；内置供应商沿用 SDK ID。运行期间拒绝编辑、删除或切换模型，避免流式请求中途改变配置。空闲会话切换模型使用 SDK `session.setModel()`，不重建或删除会话历史。

**核心流程**：用户从应用侧栏进入“设置”，在设置导航中打开“模型供应商”。左侧页内标签列表用于搜索、选择已添加供应商或开始配置内置/自定义供应商，右侧始终显示当前供应商的连接参数和模型。选择内置供应商时模型区初始为空，用户填写密钥后点击“测试连接并拉取模型”：连接测试只校验协议、服务地址、认证和请求头，不要求用户预先填写模型；OpenAI 兼容接口读取 `/models`，Anthropic 读取 `/v1/models`，Google 读取 `/models`。Base URL 和 API Key 下方即时显示可确定的成功或失败状态；发现结果显示总数，支持搜索并在固定高度列表中逐个加入，已配置模型也在固定高度区域内编辑，避免模型过多时拉长整个页面。接口不支持发现或测试失败时仍可手动填写模型；至少有一个模型后才能保存。保存后供应商立即注册到运行时，用户再从该供应商的模型列表中明确激活当前模型。删除正在使用的唯一供应商时会清空当前模型并释放内存中的运行时会话，但不会删除磁盘上的 JSONL 会话记录。

**涉及模块/文件**：`src/renderer/src/components/SettingsView.vue` 提供页内供应商标签、参数配置、字段级连接反馈、模型与请求头编辑、推理提示、空状态和运行锁定提示；`src/renderer/src/styles.css` 定义双栏及窄窗口布局；`stores/agent.ts` 管理供应商目录与激活模型状态；`src/shared/contracts.ts` 定义供应商档案、草稿、协议、连接结果和 IPC；`src/preload/index.ts` 暴露固定桥接；`src/main/ipc.ts` 在主进程入口校验输入；`settings-store.ts` 负责设置迁移、加密存储和幂等更新；`provider-discovery.ts` 负责模型发现、错误转换和字段归因；`agent-runtime.ts` 负责 SDK 目录、注册、密钥注入、模型切换和会话释放。

**配置项**：所有公开配置继续保存在 `app.getPath("userData")/settings.json`。供应商 API Key 保存为独立的加密字段；请求头名称以明文保存用于界面展示，所有请求头值序列化后整体加密。解密仅发生在 Electron 主进程，秘密值只进入连接测试请求和 `ModelRuntime` 内存，不写入 SDK `models.json`、会话 JSONL、renderer 状态或错误信息。连接测试超时为 15 秒，目前没有环境变量或特性开关。

**变更记录**：2026-07-24 21:09 CST，完成设置 v2、旧配置迁移、多供应商加密存储、SDK 内置目录、自定义供应商注册、四协议连接测试与模型发现、会话内模型切换、双栏中文设置界面及相关单元/本地 HTTP 集成测试。2026-07-24 21:24 CST，将侧栏入口统一为“设置”，设置中心左侧改为可扩展页面导航，并将供应商日常列表收纳为带搜索和固定滚动区的下拉选择器。2026-07-24 21:38 CST，重构供应商选择和添加流程为统一下拉面板，并让自定义供应商表单在首次使用时默认展示。2026-07-25 13:40 CST，将供应商配置从弹窗迁入正式工作区设置视图，保留运行锁定、连接测试、模型发现与激活行为。2026-07-25 14:49 CST，将供应商选择改为页内左侧标签列表，增加字段级连接反馈与推理选项 Tooltip，并补充窄窗口布局。2026-07-25 15:04 CST，移除内置供应商的静态模型列表，改为用户连接后主动拉取模型。2026-07-25 15:29 CST，拆分连接测试与保存校验，修复首次拉取模型被“至少配置一个模型”拦截的问题；模型发现和已配置模型改用可搜索或固定高度的滚动区域。

## Codex 风格任务工作台

**状态**：已完成（2026-07-25 15:04 CST）

**需求背景**：原 renderer 将会话、消息、审批和供应商设置集中在单个 `App.vue` 中，视觉上接近通用聊天应用，运行过程与本地项目状态不够清晰。新版需要采用已确认的 Codex 0.145 桌面交互方向，并使用统一的 LOOP 品牌。

**设计思路**：renderer 拆分为应用外壳、项目与会话导航、任务工作区、输入区和设置视图。中央内容使用无卡片消息流，工具事件按连续调用链展示；输入区常驻显示项目、当前模型和真实执行合同。工作区在 1100px 以下收缩为图标栏，长路径、会话名、工具摘要和错误均支持截断或换行。

**关键决策**：首次工作台改版只替换 renderer；P0 可信运行主链路随后将其接入真实运行合同，移除过渡期审批界面。品牌名称、窗口标题和打包显示名统一为“LOOP”，图标采用 `2pi-logo-options.html` 中的“A / 回环”；工作侧栏不再占用顶部空间展示字标，回环标记转移到输入区的发送与中止动作中。为保证本地设置和会话升级后继续可见，`2pi-agent` 包名、`cn.2pi.agent` 应用 ID 与历史 custom entry 标识保持不变。

**核心流程**：启动后加载本地设置、供应商目录和会话记录；用户从左侧选择项目、新建或恢复任务，在底部输入区发送要求。流式回复进入中央记录，工具调用显示运行中、完成或失败状态；用户可中止任务，并从侧栏进入工作区内设置页管理供应商、模型与 Agent 参数。

**涉及模块/文件**：`src/renderer/src/App.vue` 负责视图切换和初始化；`components/AppSidebar.vue` 管理项目与会话导航；`LoopMark.vue` 提供品牌图标；`TaskWorkspace.vue` 展示任务记录、工具链、审批和错误；`PromptComposer.vue` 管理发送、停止及当前运行合同；`SettingsView.vue` 管理供应商；`stores/agent.ts` 将 IPC 事件归一化为界面状态；`styles.css` 定义视觉令牌和响应规则；`src/renderer/index.html`、`src/main/index.ts` 与 `package.json` 定义正式品牌标题。

**配置项**：工作台自身不单独持久化配置。当前读取 Electron `userData/settings.json` 的 v3 设置和 `userData/sessions` 下的 JSONL 会话；renderer 不直接读取密钥或项目文件。

**变更记录**：2026-07-25 13:40 CST，正式替换为 Codex 风格任务工作台，拆分 renderer 模块，新增工具状态链、工作区内设置视图和窄窗口图标栏。2026-07-25 15:04 CST，品牌名称更新为 LOOP，接入“A / 回环”品牌图标，并同步窗口标题和打包显示名。2026-07-25 15:29 CST，输入区发送动作改用回环标记，运行中保持旋转并以红色背景明确表示可中止；移除侧栏顶部图标与文字，使新任务、项目和记录内容整体上移。

## pi 0.81.1 能力与体验补齐路线图

**状态**：已完成（2026-07-25 17:35 CST，第四期 P2 已完成）

**需求背景**：项目已经固定使用 `@earendil-works/pi-ai`、`@earendil-works/pi-agent-core` 和 `@earendil-works/pi-coding-agent` 0.81.1，但桌面端目前只接入文本增量、七个基础工具、单线会话、任务取消和写入/命令审批。SDK 已提供更完整的运行事件、思考档位、自动重试、用量与上下文信息、任务中追加消息、会话压缩与分支、技能、提示模板、扩展以及多模态消息等能力。继续补齐时，需要优先解决“运行状态不可完整恢复”和“界面控制尚未真正约束运行时”这两个基础问题，避免只增加按钮而没有可靠的底层语义。

**设计思路**：继续以主进程 `AgentRuntime` 作为 SDK 的唯一边界，通过共享类型和固定 IPC 向 renderer 提供稳定能力，不让界面直接依赖三套 SDK 类型。能力按依赖关系分四期推进：第一期先建立可恢复的任务时间线和真实执行合同；第二期开放 SDK 已有的技能、提示模板、任务追加、重试和上下文管理；第三期补齐会话分支、压缩、历史管理与改动审阅；第四期再扩展图像输入和更多模型协议。`pi-ai` 负责模型能力、流式消息、思考档位和用量；`pi-agent-core` 负责回合、工具、重试等生命周期；`pi-coding-agent` 负责会话 JSONL、资源加载、扩展、技能和压缩。新的一方能力优先通过 `DefaultResourceLoader` 或内联扩展接入，不把技能、命令或审批规则写死到 Agent 循环。

**关键决策**：

- 第一优先级是统一任务数据模型。会话快照不再只还原用户和助手纯文本，而是按回合保留文本、工具、思考生命周期、审批、重试、压缩、错误和用量状态；工具记录归属具体回合，不再作为当前页面的一组全局临时数据。0.81.1 的 thinking 事件包含模型原始内容而不是安全摘要，因此界面不转发或保存该原文，只根据开始、结束事件显示“正在思考”和耗时等可观察状态。
- 执行档位以 `PRODUCT.md` 的产品约定为准，正式提供“只读”和“全自动”两档，并允许在高级设置中关闭具体工具。只读档在主进程工具允许列表中强制执行；全自动档不逐次审批。当前“受控执行 + 每次审批”作为 MVP 过渡行为，在第一期完成前保留，迁移完成后退出主流程。
- 全局 Agent 参数写入设置文件，不按会话重复保存；每次任务开始时生成不可变的运行合同，运行中不允许切换模型、档位、思考级别或工具集合，变更从下一次任务生效。保存与 IPC 命令都要保持幂等，重复点击不能重复分支、重复压缩或重复发送排队消息。
- 先开放 Markdown 型资源，再开放可执行扩展。技能、提示模板和项目上下文可从用户目录与项目 `.pi` 目录加载；项目内 TypeScript/JavaScript 扩展可以执行任意本地代码，必须经过明确的项目目录信任确认并使用 SDK 的路径级信任记录。界面需要说明：目录获信任后，其中后续新增或修改的资源也会被加载，并提供撤销信任入口。信任只是风险确认，不宣称提供沙箱隔离。
- 会话分支使用 SDK 的追加式树结构，原分支不删除；压缩使用 SDK 原生 compaction entry，不自行改写历史 JSONL。任务运行中的新消息明确区分“立即引导”和“排队继续”，避免把两种语义混在同一个发送动作中。
- 图像输入必须由模型的 `input` 能力决定是否可用，并与会话一起留在本地。近期不接入 OAuth、图片生成、RPC 子进程、服务端、数据库、沙箱/容器、原始思维链、WebSocket 缓存传输和全部供应商私有兼容参数。

**核心流程**：

1. **第一期 P0：可信运行主链路（已完成）。** 将共享事件升级为回合/条目时间线，完整转换文本、工具开始/更新/结束、思考开始/结束、自动重试、失败、取消、用量和上下文占用；会话恢复后得到与运行结束时一致的可见记录。设置升级为 v3，新增执行档位、思考级别、自动重试和工具开关；主进程按运行合同创建会话。只读档无法执行写入或命令，全自动档无需审批即可执行允许的工具，renderer 无法获得原始 thinking 文本，重启后工具和错误记录仍可查看，同一终止事件不会让任务重复完成。
2. **第二期 P1：长任务控制与资源扩展（已完成）。** 在任务运行时允许用户选择“立即引导”或“排队继续”，展示队列并支持幂等删除；显示自动重试次数、上下文占用，并允许空闲时手动压缩。设置页增加“Agent 资源”，列出已加载的技能、提示模板、扩展和诊断信息；支持用户级与项目级标准目录，项目可执行扩展先经过信任确认。追加消息顺序由 SDK 原生队列保证，取消任务会清理待处理交互；资源加载失败会降级到基础会话，同名或失效资源会形成可见诊断。
3. **第三期 P1：会话树与改动审阅（已完成）。** 提供按项目分组的历史搜索、重命名、会话树查看、从历史节点分支和原生分支摘要；分支操作保持原历史可回退。增加基于当前工作目录的只读 Git diff 审阅面板，将文件修改工具与实际改动关联，支持按文件查看但不承担 Git 暂存或回滚。分支后上下文与所选节点一致，重复分支请求只产生一个目标，压缩和分支后重新打开仍可恢复，未初始化 Git 的项目得到明确空状态。
4. **第四期 P2：多模态与模型能力（已完成）。** 供应商模型配置补充真实的输入类型、上下文窗口、最大输出和工具调用兼容性；支持选择或粘贴图片作为用户消息输入，历史恢复时保留图片，并根据模型能力禁用不支持的发送。图片以 SDK 消息内容嵌入会话 JSONL，不另建附件缓存；主进程限制为单张不超过 8 MiB、每条消息最多 4 张，并只接受 PNG、JPEG、WebP 和 GIF。模型发现会从供应商返回的可用字段和已知模型命名推断可编辑的初始能力，用户可在模型配置中确认或调整。当前协议仍限定为适合 API Key 或本地地址的 OpenAI Chat、OpenAI Responses、Anthropic Messages 和 Google Generative AI，不引入 OAuth 流程。纯文本会话保持原行为，密钥与请求头仍不进入 renderer 或 JSONL。

**涉及模块/文件**：第一期主要涉及 `src/shared/contracts.ts`、`src/main/runtime/agent-runtime.ts`、`event-mapper.ts`、`settings-store.ts`、`validation.ts`、`src/main/ipc.ts`、`src/preload/index.ts`、`src/renderer/src/stores/agent.ts`、`TaskWorkspace.vue`、`PromptComposer.vue` 和对应测试；第二期增加资源与信任相关的主进程适配、设置视图和资源诊断组件；第三期增加会话树/历史 IPC、侧栏管理和 diff 审阅区域；第四期扩展供应商模型类型、附件 IPC、输入区和会话快照转换。每一期完成时同步更新本节状态、涉及文件和变更记录。

**配置项**：第一期已将 `app.getPath("userData")/settings.json` 从 v2 幂等迁移到 v3，并新增全局 `executionMode`、`thinkingLevel`、`autoRetry` 和 `enabledTools`。新安装默认全自动；已有 v2 配置迁移后先进入只读档并提示用户确认，避免原本需要审批的写入在升级后静默自动执行。会话继续以 SDK JSONL 保存在 `app.getPath("userData")/sessions`，分支、压缩、用量和多模态消息跟随会话生命周期；图片以 Base64 消息内容随 JSONL 保存，不上传到 LOOP 自有服务，也不产生独立缓存，直到对应会话文件或整个应用数据被删除。用户级技能、提示模板和扩展位于 `app.getPath("userData")/agent` 下的 SDK 标准子目录，项目级资源位于工作目录 `.pi`；SDK 的路径级项目信任记录保存在 `app.getPath("userData")/agent/trust.json`，由用户主动撤销或清除应用数据时结束。当前不新增环境变量、远端配置、数据库或特性开关。

**变更记录**：2026-07-25 14:05 CST，完成 0.81.1 能力盘点并确定四期路线：先补可信运行与执行档位，再补资源扩展和长任务控制，随后补会话树与改动审阅，最后接入多模态和更多模型能力；明确 OAuth、沙箱、RPC 子进程和图片生成不在当前范围。2026-07-25 14:35 CST，完成第一期 P0；其独立设计记录见下节。2026-07-25 16:02 CST，完成第二期 P1；加入运行中消息控制、手动压缩、资源清单与项目资源信任，独立设计记录见下节。2026-07-25 16:40 CST，完成第三期 P1；加入历史搜索与重命名、追加式会话分支树、分支摘要和只读 Git 改动审阅，独立设计记录见下节。2026-07-25 17:35 CST，完成第四期 P2；加入模型能力元数据、图片选择和粘贴、主进程附件校验、SDK JSONL 图片持久化与恢复，并补充模型发现能力初始值。

## 第四期 P2：多模态与模型能力

**状态**：已完成（2026-07-25 17:35 CST）

**需求背景**：文本任务无法让用户直接交给 Agent 截图、设计稿或错误界面；同时模型配置此前把输入能力、上下文窗口和最大输出固定为运行时默认值，无法反映真实服务能力。

**设计思路**：供应商中的每个模型保存文本/图片输入能力、上下文窗口、最大输出、推理与工具调用标记，并原样注册到 SDK。输入区在当前模型支持图片时提供文件选择和剪贴板粘贴，图片转换为 Base64 消息内容后随 `AgentSession.prompt()` 写入现有 JSONL。会话时间线只投影图片 MIME 与内容用于本地预览，Agent 运行仍只通过预加载桥接发起。

**关键决策**：图片在 renderer 可被选择和预览，但 IPC 入口不信任 renderer 数据，主进程重新核验数量、Base64 完整性、解码后尺寸和 MIME；仅允许 PNG、JPEG、WebP、GIF，单张上限 8 MiB、每条消息最多 4 张。运行中的追加队列继续只支持文本，避免在 SDK 已排队接口中丢失图片附件。图片不复制到独立缓存或项目目录，因此删除对应会话 JSONL 或清除应用数据会同时删除图片。

**核心流程**：用户在模型设置中连接供应商并拉取模型，点击某个模型即自动将它加入供应商并启用；仍可手动配置模型能力。选择具备图片输入能力的模型后，在输入区点击图片按钮或直接粘贴图片。界面显示最多四张待发送缩略图；发送时 renderer 将文本和图片交给 IPC，主进程校验后确认模型能力并调用 SDK。重开会话时，时间线从 JSONL 中读取用户消息的图片内容并本地显示；不支持图片的模型会禁用添加和发送图片。

**涉及模块/文件**：`src/shared/contracts.ts` 定义图片、输入和模型能力合同；`validation.ts`、`ipc.ts`、`preload/index.ts` 与 `agent-runtime.ts` 负责附件校验、IPC、SDK 转换和模型注册；`provider-discovery.ts` 读取模型发现元数据；`settings-store.ts` 迁移旧模型默认值；`session-timeline.ts` 恢复图片消息；`PromptComposer.vue`、`TaskWorkspace.vue`、`SettingsView.vue` 与 `styles.css` 提供中文输入、预览、历史和能力编辑界面。相关测试覆盖模型发现、运行时注册、附件输入校验和 JSONL 恢复。

**配置项**：模型能力随供应商公开配置保存于 `app.getPath("userData")/settings.json`；旧配置会自动得到文本输入、128K 上下文、16K 最大输出和允许工具调用的默认值。图片仅以 Base64 用户消息嵌入 `app.getPath("userData")/sessions` 下的 SDK JSONL，无独立缓存、环境变量或远端服务；API Key 与请求头仍是主进程加密设置，绝不写入 JSONL。应用优先使用 Electron `safeStorage`；Linux 没有可用 Secret Service/KWallet 时，使用 `app.getPath("userData")/secret.key` 中权限为当前用户读写的随机密钥进行 AES-256-GCM 后备加密。该后备方案保护设置文件不出现明文，但安全强度低于操作系统密钥环；删除 `secret.key` 后，以它加密的密钥将无法恢复。

**变更记录**：2026-07-25 17:35 CST，完成多模态图片输入、图片会话恢复、主进程 8 MiB/4 张/MIME 校验、模型输入类型与上下文/输出能力配置，以及相关测试。2026-07-25 17:45 CST，模型发现列表改为点击即保存、添加并启用，移除额外确认步骤。2026-07-25 18:00 CST，为无系统密钥环的 Linux 环境增加用户目录 AES-256-GCM 后备加密，避免 `safeStorage` 不可用时无法保存模型密钥。

## 第一期 P0：可信运行主链路

**状态**：已完成（2026-07-25 14:35 CST）

**需求背景**：旧版界面只能稳定显示文本和当前进程内的零散工具事件，重启后无法完整恢复工具、错误和运行结果；“受控执行”文案也没有形成可验证的运行合同。第一期需要先让用户看到的状态与主进程真实执行保持一致，再继续增加长任务和资源扩展能力。

**设计思路**：共享合同新增不可变的 `RunContract` 和按回合组织的 `TimelineTurn`。每次运行开始时，主进程冻结模型、工作目录、执行档位、思考级别、自动重试和工具集合；renderer 只消费稳定事件，不依赖 SDK 内部类型。实时事件与会话恢复共用相同的可见语义，`session-timeline.ts` 从 SDK JSONL 当前分支恢复文本、思考生命周期、工具、重试、错误、终止状态和用量。

**关键决策**：只读限制必须在主进程工具允许列表执行，不能依赖界面禁用按钮；即使高级设置误选，只读档也会强制排除 `bash`、`edit`、`write`。全自动档不逐次审批，工具仍可在高级设置中单独关闭。0.81.1 暴露的 thinking 事件可能包含模型原始思维内容，因此主进程只转发开始、结束与耗时，原文不写入 LOOP 自定义记录，也不交给 renderer。SDK 没有持久化的自动重试状态使用 custom entry 补充，其余历史优先从原生 JSONL 重建。

**核心流程**：用户在“Agent 设置”选择执行档位、思考级别、自动重试和可用工具。发送任务时，主进程确认当前没有其他运行，按当时设置生成运行合同并创建或恢复 SDK 会话。界面为本次用户消息建立一个回合，随后把文本、思考状态、工具调用、重试和用量归入该回合。运行中禁止切换会话、工作目录、模型或 Agent 参数；完成、失败或取消只结算一次。重新打开会话时，主进程从 JSONL 当前分支重建相同结构，未结束的工具错误恢复为失败而不是取消。

**涉及模块/文件**：`src/shared/contracts.ts` 定义 v3 设置、运行合同、时间线和事件；`src/main/runtime/agent-runtime.ts` 冻结运行合同、过滤工具并管理终止状态；`event-mapper.ts` 转换 SDK 事件且过滤 thinking 原文；`session-timeline.ts` 从 JSONL 恢复时间线；`settings-store.ts` 负责默认值与 v1/v2 迁移；`validation.ts` 和 `src/main/ipc.ts` 校验并锁定入口；`src/preload/index.ts` 暴露固定桥接；`stores/agent.ts` 管理回合状态及发送竞态；`TaskWorkspace.vue`、`PromptComposer.vue`、`SettingsView.vue` 展示时间线、运行合同和 Agent 设置。相关测试位于 `tests` 与 `src/renderer/src/stores/agent.test.ts`。

**配置项**：全局设置保存在 Electron `app.getPath("userData")/settings.json`，版本为 v3；`agent` 字段包含 `executionMode`、`thinkingLevel`、`autoRetry` 和 `enabledTools`。新安装默认全自动；v1/v2 用户升级后先进入只读档，并通过 `agentNeedsConfirmation` 提示确认，用户保存一次 Agent 设置后清除提示。会话与可恢复时间线继续保存在 `app.getPath("userData")/sessions` 的 SDK JSONL 文件中，自动重试作为不含秘密和 thinking 原文的 custom entry 保存；删除会话文件或清除应用数据后随之消失。当前不新增环境变量、远端存储、数据库或特性开关。

**变更记录**：2026-07-25 14:35 CST，完成设置 v3 与安全迁移、只读/全自动运行合同、工具开关、思考级别、自动重试、按回合时间线、JSONL 恢复、终止幂等、运行期间配置锁定和中文状态界面；移除逐次审批主链路及其旧实现。

## 第二期 P1：长任务控制与资源扩展

**状态**：已完成（2026-07-25 16:02 CST）

**需求背景**：长任务运行时，用户过去只能等待或中止，无法补充约束、安排下一项工作，也无法主动控制上下文长度。SDK 支持的技能、提示模板、扩展和项目说明此前虽然可能被底层加载，但界面无法确认实际生效内容、加载错误或项目代码是否经过信任。

**设计思路**：运行中的追加消息直接使用 SDK 原生的两类队列。“立即引导”在当前工具调用结束后优先进入下一轮，“排队继续”在 Agent 原本准备结束时继续执行。主进程为每条待处理消息保留稳定 ID，并串行处理新增、删除和取消，防止重复点击或并发调用造成重排。手动压缩只允许在空闲会话执行，并使用 SDK 原生压缩条目保留摘要。资源能力通过 `DefaultResourceLoader` 接入，主进程把已加载资源和诊断转换为稳定共享类型，renderer 不直接依赖 SDK 类型。

**关键决策**：待处理消息只在当前运行进程内存在；消息被消费后作为普通用户消息写入会话 JSONL，取消任务会同时清空尚未消费的两类队列。重复消息请求按客户端 ID 去重，重复删除直接返回当前队列，压缩中的重复请求复用同一次操作。项目资源采用 SDK 的路径级信任记录；未信任时不加载项目级技能、提示模板和扩展。信任意味着该路径及后续新增或修改的项目扩展可以执行本地代码，不代表沙箱隔离。单个扩展或资源加载失败会形成诊断；若加载器整体失败，则先停用扩展重试，仍失败时以无外部资源的基础会话启动。

**核心流程**：任务运行后，输入区保持可编辑，用户选择“立即引导”或“排队继续”并提交；消息进入可见队列，可在消费前单条删除。SDK 开始处理某条追加消息时，界面将上一回合结算并建立新的时间线回合；任务取消、失败或完成时清空待处理状态。空闲时可从工作区标题栏压缩上下文。设置页的“Agent 资源”列出用户级和项目级资源、标准目录与加载诊断；检测到项目资源但尚未信任时，用户确认风险后写入信任记录并重新绑定会话，也可随时撤销。

**涉及模块/文件**：`src/shared/contracts.ts` 定义队列、压缩、资源和信任合同；`src/main/ipc.ts` 与 `src/preload/index.ts` 提供固定 IPC；`agent-runtime.ts` 管理 SDK 队列、压缩、会话锁和资源重载；`run-queue.ts` 负责队列镜像、去重、消费与幂等删除；`agent-resources.ts` 负责标准目录、信任、资源快照、诊断和降级；renderer 的 `stores/agent.ts`、`PromptComposer.vue`、`TaskWorkspace.vue`、`SettingsView.vue` 展示对应状态和操作。相关测试位于 `tests/run-queue.test.ts`、`tests/agent-resources.test.ts` 和 renderer store 测试。

**配置项**：不新增环境变量、数据库或远端配置。用户级资源位于 Electron `userData/agent` 下的 SDK 标准子目录；项目级资源位于工作目录 `.pi`，项目及父目录中的 `.agents/skills` 也按 SDK 规则发现。路径级信任保存在 `userData/agent/trust.json`，由用户撤销或清除应用数据时结束。已消费的追加消息和压缩摘要随 `userData/sessions` 下对应 JSONL 会话持续存在；尚未消费的队列只保存在内存中，取消任务或应用退出后消失。

**变更记录**：2026-07-25 16:02 CST，完成运行中立即引导与排队继续、可见队列、幂等删除和取消清理；接入空闲手动压缩；新增 Agent 资源清单、标准目录、加载诊断、项目路径信任与撤销入口；切换工作目录时退出旧会话绑定，避免任务与资源仍指向旧路径；移除侧栏无交互的“本地工作区”占位。

## 第三期 P1：会话树与改动审阅

**状态**：已完成（2026-07-25 16:40 CST）

**需求背景**：随着会话变长，用户需要定位过去的任务、给历史会话命名，并从可靠的历史节点尝试另一种方案，同时保留原路线。Agent 修改项目后，用户也需要在应用内核对当前工作目录的实际 Git 改动，而不是只依赖工具调用文字或最终回复。

**设计思路**：历史列表继续使用 SDK 的本地会话目录，主进程基于 `SessionManager.listAll()` 提供全文搜索和追加式名称更新。会话树从 SDK JSONL 的父子节点关系投影为稳定共享类型，renderer 不读取 SDK 条目。分支使用 SDK 原生 `navigateTree()` 与 `branch_summary`，并用两阶段请求标记和节点标签保证重复 IPC 或异常重试不会创建重复目标。Git 审阅由独立只读适配器执行参数化命令，读取 porcelain 状态以及按文件的暂存、未暂存和未跟踪 diff。

**关键决策**：分支留在同一个追加式 JSONL 树中，不复制、删除或改写原分支；每个分支请求先写入 pending 标记，摘要节点再附加稳定请求标签，最后写入 complete 标记。若应用在摘要与 complete 标记之间中断，重开后可通过标签恢复结果。分支摘要进入后续模型上下文，用来保留放弃路径的有效信息。会话名称也是追加式元数据。跨项目打开历史会话时，先完成新资源、SDK 会话和工作目录设置的准备，再一次性替换旧会话；任一步失败都保留原会话和原目录。Git 面板只读，不提供暂存、提交或回滚；使用 `--` 和参数数组传递路径，不拼接 shell 字符串。单个 diff 限制为 1 MiB，最多读取 200 个文件，并明确显示截断、二进制、无 Git、未安装 Git和无改动状态。

**核心流程**：用户在侧栏输入关键词，主进程搜索会话名称、首条消息、全文和工作目录；结果按项目分组，可在空闲时重命名或打开。打开会话后，用户从标题栏进入会话树，选择非用户消息节点创建分支；主进程生成原生分支摘要并切换当前上下文，原路径仍留在树中。用户也可进入“改动审阅”，应用读取当前会话工作目录或当前项目目录的 Git 状态，按文件切换查看已暂存、工作区和未跟踪 diff，并显示与 `edit`、`write` 工具目标路径的关联数量。

**涉及模块/文件**：`src/shared/contracts.ts` 定义历史、树、分支和 Git diff 合同；`src/main/runtime/session-history.ts` 转换 SDK 树并保存分支幂等状态；`git-diff.ts` 封装只读 Git 命令、限制和错误状态；`agent-runtime.ts` 管理搜索、重命名、分支和工作目录边界；`settings-store.ts` 保证工作目录写盘失败时恢复内存状态；`src/main/ipc.ts` 与 `src/preload/index.ts` 提供固定 IPC；renderer 的 `stores/agent.ts` 管理搜索竞态、分支锁和审阅状态；`AppSidebar.vue` 提供项目分组历史、搜索和重命名；`TaskWorkspace.vue` 提供会话树与改动审阅面板；`styles.css` 定义双栏、窄窗口和 diff 行布局。相关测试位于 `tests/session-history.test.ts`、`tests/git-diff.test.ts`、`tests/session-persistence.test.ts`、`tests/event-mapper.test.ts`、`tests/provider-runtime.test.ts`、`tests/settings-store.test.ts` 和 renderer store 测试。

**配置项**：不新增环境变量、数据库、远端配置或设置文件字段。会话名称、树、分支摘要、请求状态和节点标签都跟随对应 JSONL 保存在 Electron `userData/sessions`；删除会话文件或清除应用数据后一起消失。Git 数据不持久化到 LOOP，打开或刷新面板时从工作目录即时读取；diff 只进入当前 renderer 内存，关闭应用后消失。文件修改工具只将目标路径保存到既有会话工具记录中，用于与实际 Git 改动关联，不复制项目内容。

**变更记录**：2026-07-25 16:40 CST，完成按项目分组的全文历史搜索、会话重命名、可恢复的追加式会话树、带原生摘要和两阶段幂等标记的历史分支、按文件的只读 Git 改动审阅、工具目标关联、非 Git 与无改动空状态及相关测试。2026-07-25 16:49 CST，补充跨项目历史会话的原子切换和写盘失败回滚，避免会话与工作目录状态错位。
